#include <stdio.h>
#include <string.h>

#include "change.h"
int main() {
 //   system("chcp 65001");
    char str[1001],str1[1001];
    scanf("%s",str);
//    strcpy(str1,UTF8ToANSI(str));
    printf("%s",str);
}